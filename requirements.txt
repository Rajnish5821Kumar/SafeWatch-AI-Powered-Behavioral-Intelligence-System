# SafeWatch - AI Behavioral Intelligence System
# Core requirements

# ─── Computer Vision ───────────────────────────────────────────────────────────
ultralytics>=8.2.0           # YOLOv8 detection + pose estimation
opencv-python>=4.8.0         # Video I/O, frame processing
supervision>=0.19.0          # ByteTrack wrapper, annotation utilities
Pillow>=10.0.0               # Image utilities (pre-built wheel)

# ─── Deep Learning ─────────────────────────────────────────────────────────────
torch>=2.1.0                 # PyTorch (CUDA-compatible)
torchvision>=0.16.0
# tensorflow and keras are optional — only needed to train/run LSTM Autoencoder
# Install separately if needed: pip install tensorflow>=2.14.0
# tensorflow>=2.14.0
# keras>=2.14.0

# ─── Emotion Recognition ───────────────────────────────────────────────────────
fer>=22.5.1                  # Facial Emotion Recognition (CNN-based)
# deepface is optional (heavier install) — uncomment if needed:
# deepface>=0.0.89

# ─── Anomaly Detection / ML ────────────────────────────────────────────────────
scikit-learn>=1.3.0          # IsolationForest, StandardScaler, PCA
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# ─── API Backend ───────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # File upload support
websockets>=12.0
aiofiles>=23.2.1
pydantic>=2.5.0
httpx>=0.27.0

# ─── Utilities ─────────────────────────────────────────────────────────────────
PyYAML>=6.0.0                # Config loading
loguru>=0.7.0                # Structured logging
tqdm>=4.65.0                 # Progress bars
python-dotenv>=1.0.0
rich>=13.0.0                 # Pretty terminal output
